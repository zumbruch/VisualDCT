-unified configuration files searcher 
-persistence of the selected tab in spreadsheet view
-inspector has to refresh on field change in spreadsheet
-inspector has to refresh link fields on rename in main window
-window unfocus, focus on rename dialog should not overwrite the name with the current
-in the main vdct workspace the grid should be displayed to the exact bottom and right edge
-preservation of unrecognised view comment data for future versions
-deleting instances in spreadsheet using delete key
-detecting all hidden/shown row states for spreadsheet row popup command selection
-TASK:NONMODALSPR: nonmodal spreadsheet view
-generalize inspector interface for both inspector and spreadsheetinspector
-TASK:EMPTYNULL: strict usage of ""/null with property values, including comment property
-TASK:UNEDITCOM: uneditable comment property
-TASK:COLREUSE: reusing of generated columns in spreadsheetcolumnmodelview
-TASK:SCALING: remove all hardcoded scale limits
-TASK:SNAP: pixel precise grid snapping
-TASK:MACROSER: serialization of macros without visual representation
-TASK:SLASH: consistent usage of only one type of slashes in filenames
-TASK:STRUTILOPT: optimize string operation utilities
-TASK:RECMACROS: support for recursive macro substitutions
-TASK:VISREPACT: changes to data objects requiring gui events go trough visual representations
-TASK:RDBDBD: define rdb dbd defintions vs file definitons
-TASK:RDBTEMPL: define rdb template usage
-TASK:RECCODES: acknowledge n/e record code states
--------------------------------------------------------------------
-vdct package-f
 -general unknowns
  -id of template only filename?
---
 -basic user usecases

  -uncommited changes dialogs
   -check all positions of warning dialogs
   -all done except
    -on close vdct
     -warn as on close all
     -warn also about changed templates

  -refreshing workspace data
   -refreshing magnification slider at switching ds
   -refresh template desc at window focus change/close/new

  -also load description to id
  -rec_type_code n/e distinction

  -check code for disposing managers on int frame closed and test
   -inspector manger does not dispose of inspectors on ds close
   -test inspector adding, removing instances
   -use inspectors, some frozen, on more ds and check creation

  -saving with incomplete data
   -merge new rdbdataid and rdbchooser dialogs
    -listner on tree writes into editing fields
    -on load mode disable editing of fields
   -conversion from partial template to rdbdataid
   -bring up dialog to insert missing data
    -fill in guesses for the values
   -replace current id data at saveas

  -template instance copy
   -extract template instance cycling check if necessary
    -apply to copy template instance check
   -when template not exising in repository, try to to load it
    -inform when this fails
    -apply to copy

  -enable cut/copy/pasting macros/ports in/between templates
---
 -nonfunctional improvements
   -pasting always relative to the prev paste, position
    -on separate ds copy:
     -position of the first record should be in the view centre
    -on cut on the same ds first same pos, then offsets

   -position management: displace at each new frame
   -navigator position is not refreshed at deiconify
   -setdefaultlookandfeeldecorated for all frames
   -local group view copies the previous if nonexisting
   -start window large or maximized
   -empty window at startup only when no loading
   -nonblinking on main window resize
   -move renaming does not check the existence in the current group
   -view is not refreshed at load from db
   -snap to grid before displaying objects on copy

  -window creation on successful loading
   -opening database files when ds not present
   -separate data conversions and checking to dsmanager
    -setting cursors
    -setting undo monitoring
    -consistency checks
    -garbage collecion
   -garbage collection in finalize

  -database gui improvements
   -display descriptions of rdbdatas in the tree
   -make tree dialog less wide
   -state dialog when more time can pass

  -database access
   -structural database access updates
    -create id retrieve id discrepancy
     -use last inserted id for this
     -generic getlast function for oracle compatibility

   -warning messages can be too long in dialogs
  
  -empty undo action when importing files/undoable import
   -also check loading/saving from rdb
  -switch message and title on printing error dialogs
  
  -think about removing instances at gui remove
   -maybe this works automatically when instances are autogenerated

---
  -documentation
   -code documentation
    -one clipboard
     -all roots work with a single clipboard gotten by name
    -copy/paste functionality with a single active group
     -copy always copies to clipboard, active group ignored
     -paste always moves from clipboard to active group
---
 -nondefinite improvements
   -finder that searches in all windows
   -solve console popups at text display
   -loading multiple selected files
   -enable tree view state preservation
   -optional password saving to file
   -workspacedesktop moves its windows' title area inside on resize
---
  -delayed
   -database access optimization
    -additive to start remove database access
     -single row: .5d
     -multirow: 1.5d
     -speed up: currently two queries per field
    -faster query/insert/update with table operations
    -generic managerow function
    -extend to 2d values passing

   -improving database access guis
    -removing/renaming db records in the database, new iocs
     -adding of new iocs when nonexistant in the database
     -addnewdialog has editable combobox
    -make one click less choosing versions during loading/saving

   -validation
    -check data based definition for all unknown fields
     -all tables you add to
     -specify known
     -make a list of unknown to be asked
     -include rec_type_code

   -database template support
    -database tables
     -port table foreign key group
     -template instance table foreign key group
     -macro foreign key template instance
    -functionality
     -in memory template loading from rd
    -validation
     -templates in editing mode influence on operations
    -current bugs

   -in-vdct template usage
    -same name macro/port distinction on template sync
    -consistency of templates across drawingsurfaces

   -settings saving
    -settings vs xmlsettings
--------------------------------------------------------------------
-vdct desy
 -desy requirements for vdct
  -tree view for template managment
  -integration of tree view and inspectors into JFace/SWT, Eclipse
--------------------------------------------------------------------
